<div class="container mt-5 p-4">
  <h2>Atividades Extracurriculares</h2>
   <%= render "form"%>
  <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
    <%= link_to '+Adicionar Documento', new_certificate_path, :remote => true, "data-toggle" => "modal", "data-target" => "my-modal"  %>
  </button>
  <hr>

  <table>
    <tr>
      <th>
        Documento
      </th>
      <th style="width: 50%;">
        Atividades
      </th>
      <th>
        Horas
      </th>
      <th>
        Status
      </th>

    <% @certificates.each do |certificate| %>
      <tr></tr>
      </tr>
        <td><%= link_to "#{certificate.attachment_identifier}", certificate.attachment_url %></td>
        <td><%= certificate.activity_description %></td>
        <td><%= certificate.hours %>
        <td><%= certificate.status ? "Aprovado" : "Pendente" %></td>
      <tr>
    <% end %>

    </tr>
  </table>
</div>


